# Refresh Search Button

An Anki addon that adds a refresh button (🔄) next to the search bar in the browser to quickly re-run the current search.

## Use Case

I wanted a way to manually refresh the displayed notes/cards after making a modification, e.g. adding new notes, deleting notes.
